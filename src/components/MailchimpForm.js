import { Newsletter } from "./Newsletter";

export const MailchimpForm = () => {
  // Commented out or removed the MailchimpSubscribe component and its related code.
  return (
    <>
      {/* 
      <MailchimpSubscribe
        url={postUrl}
        render={({ subscribe, status, message }) => (
          <Newsletter
            status={status}
            message={message}
            onValidated={formData => subscribe(formData)}
          />
        )}
      />
      */}
    </>
  );
};
